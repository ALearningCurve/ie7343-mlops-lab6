MODEL_BUCKET=your-model-bucket-name
MODEL_OBJECT=models/classifier.joblib
GOOGLE_CLOUD_PROJECT=your-project-id
# ADC-first: leave GOOGLE_APPLICATION_CREDENTIALS unset for local uv runs.
# For docker compose, set GOOGLE_APPLICATION_CREDENTIALS_HOST only if you need
# to override the default host ADC file path.
